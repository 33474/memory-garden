# 本地运行说明

1. 安装 [Node.js](https://nodejs.org/) 18 或更新版本。
2. 解压本文件夹。
3. 在该文件夹打开终端。
4. 执行 `npm install`。
5. 执行 `npm run dev`。
6. 打开终端显示的网址。

如果只想查看已经构建好的静态版本，可在任意静态服务器中打开 `dist` 文件夹；不要直接双击 `dist/index.html`，因为部分浏览器会限制本地文件脚本。
